# 2026-04-24 算法修复计划

> 已归并：本文档不再作为独立执行入口。后续套定额算法修复统一按 `docs/global_repair_small_step_plan_v36.md` 执行；本文只保留为历史基线和早期修复记录。

## 0. 历史接续口令（已停用）

以下口令仅作历史记录，不再使用。后续统一使用 `docs/global_repair_small_step_plan_v36.md` 的启动语。

```text
读取修复计划4.24，继续执行下一步。
```

执行时先读本文档，再读必要的目标文件。不要重新全仓库发散分析。

## 1. 执行原则

1. 每轮只处理一个问题域。
2. 每轮必须有验收命令和通过标准。
3. 未过验收不进入下一步。
4. 不同时改 LTR、召回、Picker、CGR。
5. 不让 OpenClaw 替代 Jarvis 首轮主匹配，只做二审、归因和离线优化。
6. 每轮结束必须输出：改了什么、验收结果、下一步。

## 2. 当前锁定基线

主基线：

- `reports/attribution/ltr_v2_full_20260422.json`

基线指标：

| 指标 | 门槛 |
|------|------|
| 总命中率 | `>= 33.8%` |
| `R2_LTR选错` | `< 1375` |
| `R4_Picker推翻正确` | `<= 294` |
| `R3_CGR推翻正确` | `<= 84` |
| `do_not_break.regression_guard_failed` | `false` |

参考文档：

- `docs/套定额系统诊断与修复计划.md`
- `docs/ltr_mixed_safety_dataset_design_20260422.md`
- `docs/ltr_mixed_safety_execution_plan_20260422.md`
- `docs/jarvis_accuracy_priority/search-capability-roadmap-2026-04-08.md`
- `docs/jarvis_accuracy_priority/speed-quality-mainchain-2026-04-08.md`

## 3. 当前算法判断

当前不是缺计划，而是 LTR 修复线已经推进到 mixed safety 阶段，但候选模型还没有过保护门禁。

已确认事实：

- `ltr_v2_full_20260422` 把命中率从 `25.3%` 拉到 `33.8%`。
- 召回命中率仍是 `72.5%`，说明 `R1_召回未命中` 还没有被修。
- 纯 residual R2 训练的 `round2` 回退到 `30.2%`，说明只喂 hard case 会学偏。
- 多个 mixed safety 模型已有产物，但 `do_not_break` 仍失败。

当前优先级：

1. 先把 mixed safety 的 `do_not_break` 违例看清并修到过门禁。
2. 再跑 full benchmark 判断是否能替换主基线。
3. 之后再进入 R1 召回修复。
4. 最后才做 Picker/CGR 的局部修复。

## 4. 第一阶段：LTR Mixed Safety 闭环

### 步骤 4.1 补 do-not-break 违例明细导出

目标：

让 `tools/train_ltr_v2.py` 在训练摘要中输出每个保护集失败 query 的明细，而不是只输出分组命中率。

建议输出字段：

- `query_id`
- `protect_group`
- `failure_target`
- `training_role`
- `correct_quota_id`
- `baseline_top1_id`
- `model_top1_id`
- `model_top1_score`
- `baseline_hit`
- `model_hit`
- `candidate_count`

改动范围：

- `tools/train_ltr_v2.py`
- `tests/test_train_ltr_v2.py`

验收命令：

```powershell
python -m pytest tests/test_train_ltr_v2.py -q
```

通过标准：

- 测试全绿。
- `evaluate_do_not_break()` 返回结构中包含违例明细。
- watch-only 组可以输出明细，但不能触发 `regression_guard_failed`。

### 步骤 4.2 复算现有 mixed safety 模型，拿到违例样本

目标：

用现有 mixed safety 数据复算，确认具体是哪些保护样本被打坏。

验收命令：

```powershell
python tools/train_ltr_v2.py `
  --input data/ltr_mixed_safety_training_data.csv `
  --output-dir output/ltr/model_v2_mixed_safety_diagnose `
  --do-not-break-eval reports/attribution/do_not_break_eval.json
```

通过标准：

- `output/ltr/model_v2_mixed_safety_diagnose/train_summary.json` 包含违例明细。
- 能明确最大失败组，例如 `lighting_guard`、`switch_guard` 或其他组。
- 不进入下一步前，必须在对话中列出最大失败组和前 5 个违例样本。

### 步骤 4.3 只修最大保护组

目标：

只处理一个最大失败保护组，不碰召回、Picker、CGR。

允许的修复方向：

- 调整 mixed safety 数据集的该保护组样本权重。
- 增加该保护组的 targeted seed。
- 修正明显错误或过宽的保护组标签。
- 若违例来自评估逻辑错误，先修评估逻辑。

禁止：

- 顺手改 `query_builder.py`。
- 顺手改 `pickers.py`。
- 顺手改 CGR 或 final validator。
- 为一个保护组引入全局排序规则。

验收命令：

```powershell
python -m pytest tests/test_train_ltr_v2.py tests/test_build_mixed_safety_ltr_dataset.py -q
```

通过标准：

- 最大保护组 delta 不再低于 `-0.02`。
- 其他 train_and_eval 保护组不新增失败。
- watch-only 组只观察，不阻塞。

### 步骤 4.4 训练 mixed safety 候选模型

验收命令：

```powershell
python tools/train_ltr_v2.py `
  --input data/ltr_mixed_safety_training_data.csv `
  --output-dir output/ltr/model_v2_mixed_safety_candidate `
  --do-not-break-eval reports/attribution/do_not_break_eval.json
```

通过标准：

- `do_not_break.regression_guard_failed = false`
- holdout 不明显退化。
- `train_summary.json` 记录 sample source、权重、保护组结果和违例明细。

### 步骤 4.5 跑 full benchmark 验收

验收命令：

```powershell
$env:LTR_V2_ENABLED='1'
$env:LTR_V2_MODEL_PATH='C:\Users\Administrator\Documents\trae_projects\auto-quota\output\ltr\model_v2_mixed_safety_candidate\ltr_v2_model.txt'
$env:LTR_V2_FEATURES_PATH='C:\Users\Administrator\Documents\trae_projects\auto-quota\output\ltr\model_v2_mixed_safety_candidate\ltr_v2_features.json'

python tools/run_benchmark.py `
  --profile full `
  --json-only `
  --summary-json-out output/benchmark_compare/ltr_v2_mixed_safety_candidate_summary.json `
  --attribution-json-out reports/attribution/ltr_v2_mixed_safety_candidate.json `
  --latest-result-out output/benchmark_compare/ltr_v2_mixed_safety_candidate_latest_result.json
```

通过标准：

- 总命中率 `>= 33.8%`
- `R2_LTR选错 < 1375`
- `R4_Picker推翻正确 <= 294`
- `R3_CGR推翻正确 <= 84`
- `R1_召回未命中` 不明显上涨

若失败：

- 不替换当前基线模型。
- 用 `tools/diff_benchmark_results.py` 对比主基线和候选结果。
- 只根据 diff 最大回退类型决定下一轮。

## 5. 第二阶段：R1 召回修复

进入条件：

- 第一阶段 mixed safety 候选模型通过 do-not-break。
- full benchmark 不低于主基线。

目标：

降低 `R1_召回未命中`，提高召回命中率，当前基线召回命中率是 `72.5%`。

优先顺序：

1. query rewrite / route 修正。
2. candidate pool 合并策略。
3. 全专业同义词和导入侧同义词挖掘。
4. fast miss 升级到 standard retrieve。

每轮只选一个模块。

验收：

```powershell
python tools/run_benchmark.py --profile full --json-only
```

通过标准：

- 召回命中率高于 `72.5%`。
- 总命中率不下降。
- `R2/R3/R4` 不出现明显上涨。

## 6. 第三阶段：Picker / CGR 局部修复

进入条件：

- benchmark 归因显示 `R3` 或 `R4` 成为主要剩余问题。

执行原则：

- 每次只修一个归因聚类。
- 必须有对应 regression test。
- 修 Picker 时不碰 LTR。
- 修 CGR 时不碰 Picker。

验收：

- 对应 R 类下降。
- 总命中率不下降。
- `R1/R2` 不明显上涨。

## 7. 额外改进点，但不插队

这些是有价值的改进，但不能插到第一阶段前面。

### 7.1 训练摘要需要保存违例样本 CSV

除写入 `train_summary.json` 外，建议额外输出：

- `do_not_break_violations.csv`

这样方便人工检查和后续 OpenClaw 离线 review。

### 7.2 full benchmark 结果体积过大

当前 `output/benchmark_compare/*latest_result*.json` 超过 1GB。建议后续增加 compact result：

- 只保留 `bill_id / bill_name / stored_ids / algo_id / R类 / stage top1 / recall_rank`
- 完整大文件仍可选输出

这个改进只影响工具产物，不应影响算法。

### 7.3 progress 文档要同步

`reports/agent_state/progress.json` 曾经落后于真实测试状态。后续每轮修复结束应更新“当前真实下一步”，避免接续时读到旧问题。

### 7.4 OpenClaw 只进入离线优化

OpenClaw 可用于：

- 审查 do-not-break 违例样本。
- 聚类失败原因。
- 生成小范围 patch 建议。

但不能让 OpenClaw 在线替代 Jarvis 主匹配。

## 8. 每轮结束格式

每轮结束必须输出：

```text
本轮目标：
改动文件：
验收命令：
验收结果：
是否进入下一步：
下一步：
```

## 9. 下一步

从步骤 4.1 开始：

```text
补 do-not-break 违例明细导出。
```

## 10. 当前执行进度更新（2026-04-25）

后续清理对话后，优先读取本节和最新验收记录，不要重新全仓发散分析。

已完成：
- R1-2 candidate pool merge：见 `docs/2026-04-24-R1-2-candidate-pool-merge-验收记录.md`，提交 `89f61c8`。
- R2/LTR auto diagnosis：见 `docs/2026-04-25-R2-LTR-auto-diagnosis.md`，提交 `fe812e4`。
- R2-1 LTR guard：见 `docs/2026-04-25-R2-1-LTR-guard-验收记录.md`，提交 `fce19c0`。
- R2-2a surface orientation rescue：见 `docs/2026-04-25-R2-2a-surface-orientation-rescue-验收记录.md`，提交 `08deac4`。
- R2-2b traffic arrow semantic：见 `docs/2026-04-25-R2-2b-traffic-arrow-semantic-验收记录.md`，提交 `3b76980`。
- R2-2c well diagnosis：见 `docs/2026-04-25-R2-2c-well-diagnosis-验收记录.md`。该步诊断通过，但不做代码修复。

当前最新验收基线：
- 浙江市政 full 子集：`155/541 = 28.7%`。
- R1 召回未命中：`164`，召回命中率 `69.7%`。
- R2 LTR 选错：`186`。
- R2 诊断总面：`220`。
- 最大剩余 R2 桶：`structure_signal_sparse = 105`。
- R3 + R4：`35`。

下一步：
- 进入 R2-2d，只分析 `reports/attribution/r2_2b_ltr_diagnostics.csv` 的 `structure_signal_sparse`。
- 暂不硬修 `砌筑井`；该类先等待候选族内档位/ID 映射诊断。
- 优先寻找“正确候选已在 top5，且文本显式参数能唯一指向 stored_ids”的小簇，例如 `透层/粘层` 或 `标杆`，但必须先做 stored_ids 反例检查。
- 不直接重训 LTR，不同时修改召回、CGR、Picker。

## 11. 当前执行进度更新（2026-04-26）

后续清理对话后，优先读取本节和最新验收记录：

- `docs/2026-04-26-R2-2d-bitumen-layer-rescue-验收记录.md`
- `reports/attribution/r2_2d_ltr_diagnostics_summary.json`
- `reports/attribution/r2_2d_ltr_diagnostics.csv`

已完成：

- R2-2d bitumen layer rescue：见 `docs/2026-04-26-R2-2d-bitumen-layer-rescue-验收记录.md`。

当前最新验收基线：

- 浙江市政 full 子集：`159/541 = 29.4%`。
- R1 召回未命中：`172`。
- R2 LTR 选错：`182`。
- R2 诊断总面：`216`。
- 最大剩余 R2 桶：`structure_signal_sparse = 101`。
- `ltr_bad_flip_pre_correct = 34`，未放大。
- R3 + R4：`35`。

下一步：

- 进入 R2-2e，仍只从 `structure_signal_sparse` 选择一个小簇。
- 优先考虑 `透层/粘层` 剩余家族内问题：`半刚性基层` vs `粒料基层`，但必须先确认 stored_ids 与候选文本没有互相冲突。
- 若 `透层/粘层` 剩余样本证据不足，则换一个正确候选在 top5、且清单文本显式指向 stored_ids 的稳定小簇。
- 继续禁止同轮修改 R1 召回、CGR、Picker。

## 12. 当前执行进度更新（2026-04-27）

后续清理对话后，优先读取本节和最新验收记录：

- `docs/2026-04-27-R2-2e-ltr-guard-confirmation-验收记录.md`
- `reports/attribution/r2_2e_ltr_diagnostics_summary.json`
- `reports/attribution/r2_2e_ltr_diagnostics.csv`

已完成：

- R2-2e LTR guard confirmation：见 `docs/2026-04-27-R2-2e-ltr-guard-confirmation-验收记录.md`。

当前最新验收基线：

- 浙江市政 full 子集：`160/541 = 29.6%`。
- R1 召回未命中：`164`。
- R2 LTR 选错：`181`。
- R2 诊断总面：`215`。
- 最大剩余 R2 桶：`structure_signal_sparse = 100`。
- `ltr_bad_flip_pre_correct = 34`，未放大。
- R3 + R4：`35`。
- R2-2e diff：`changed_total = 1`，`improved = 1`，`regressed = 0`。

下一步：

- 进入 R2-2f，仍只从 `structure_signal_sparse` 选择一个小簇。
- `124` 的 `半刚性基层` vs `粒料基层` 保留为候选，但必须先做 stored_ids 与候选名称反例检查，避免硬编码档位。
- 继续禁止同轮修改 R1 召回、CGR、Picker。
