# 2026-04-29 主材特征抽取-电线电缆防水-验收记录

本轮继续 `docs/plans/2026-04-29-主材查价与填价最终执行方案.md` 的第一轮主链路，范围只收敛到“先抽对主材”的小缺口。

## 修复范围

- 泛名 `配线/电线/电缆/电力电缆` 遇到项目特征里的 `名称` 和 `型号规格` 时，使用特征中的真实主材名和规格，不再用清单泛名查价。
- `材料品种/品种` 纳入主材名称字段，支持 `防水卷材` 从项目特征抽到 `SBS改性沥青防水卷材` 这类可查价名称。
- 对象类型增加 `wire/cable/cable_tray/waterproof`，并纳入价格决策强类型集合，后续候选对象不一致时可降为建议价。

## 回归用例

新增覆盖：

- `配线` + `名称:铜芯电线` + `型号规格:BV-2.5mm2`
- `电力电缆` + `名称:铜芯电力电缆` + `型号规格:YJV-4*35+1*16mm2`
- `防水卷材` + `材料品种:SBS改性沥青防水卷材` + `规格:4mm`

## 验收

已执行：

```bash
pytest tests/test_web_material_price.py tests/test_local_match_material_price.py tests/test_material_db_lookup.py tests/test_output_writer_material_gate.py -q
```

结果：`102 passed`。
