# 修复过程摘要

文件名：[安徽]1#公寓--给排水工程_wxwork_zip.xlsx

task_id：efecd9c5-2327-43e7-8534-d36339969ecf

本次修了哪些问题

- 保温类误配：补强防结露/防冻/保冷的管道保温路由，避免继续掉到普通管道安装或设备绝热。
- 刷油 / 标识类误配：为刷油、管道标识、色环建立独立 query，避免被普通安装路由抢走。
- 设备泵组 / 水箱类误配：新增设备 family picker，优先从候选中识别水箱、气压罐、变频泵组/给水设备、水泵。
- 软接头跨专业误配：修正 specialty_classifier 中“软接头”被硬覆盖到 C4 的问题，改回给排水 C10。
- 套管 / 预留孔洞 / 附配件边界混乱：收紧套管显式路由，避免“含预留孔洞/含套管/综合单价中含”这类说明把雨水斗、地漏、给排水附(配)件误导到套管或堵洞。

改了哪些文件

- src/query_builder.py
- src/explicit_equipment_family_pickers.py
- src/match_pipeline.py
- src/specialty_classifier.py
- tests/test_query_builder_pipe_routes.py
- tests/test_explicit_equipment_family_pickers.py
- tests/test_specialty_classifier_conflicts.py

为什么这么改

- `src/query_builder.py` 是这批误配最直接的检索入口。最小修法是在路由层先把刷油/标识、管道保温、套管边界分清，减少错误候选进入后续排序。
- `src/explicit_equipment_family_pickers.py` + `src/match_pipeline.py` 负责候选集内的显式家族选择，适合用极小增量补上水箱、气压罐、变频泵组/给水设备、水泵的设备家族优先级。
- `src/specialty_classifier.py` 里“软接头 -> C4”是硬覆盖错误，会直接把给排水软接头带到电气册；这是一个单点且高收益的修复。

如何验证

- 运行定向测试：

```powershell
pytest tests\test_query_builder_pipe_routes.py tests\test_query_builder_valves.py tests\test_explicit_equipment_family_pickers.py tests\test_specialty_classifier_conflicts.py
```

- 运行语法校验：

```powershell
python -m py_compile src\query_builder.py src\explicit_equipment_family_pickers.py src\match_pipeline.py src\specialty_classifier.py tests\test_query_builder_pipe_routes.py tests\test_query_builder_valves.py tests\test_explicit_equipment_family_pickers.py tests\test_specialty_classifier_conflicts.py
```

- 额外尝试运行现有 `match_pipeline` 相关测试：

```powershell
pytest tests\test_match_pipeline_installation_family_picks.py
pytest tests\test_match_pipeline_plumbing_accessory_pick.py
```

验证结果

- 定向测试通过：`43 passed`。
- 语法校验通过。
- 现有 `match_pipeline` 相关测试未能执行：仓库当前缺少 `db.sqlite` 模块，测试在导入 `src.match_pipeline` 时即失败，这属于环境问题，不是本次改动引入的回归。

还有哪些问题没修

- 没有对整份审核任务做端到端重跑，因此本次结论基于“最小源码补丁 + 定向回归测试”。
- 抗震支吊架路径本身在现有代码中已有明确管道/桥架/风管区分逻辑，本次未再做额外扩写；若后续补齐 `db.sqlite` 环境，建议补跑相关 `match_pipeline` 回归测试确认整链路表现。
