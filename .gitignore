# 环境变量（含API密钥，不上传）
.env

# Python缓存
__pycache__/
*.pyc
*.pyo
.pytest_cache/
pytest-cache-files-*/

# pip install -e . 生成的构建产物
*.egg-info/

# 数据库文件（本地生成，不上传）
db/

# 向量模型缓存
.cache/

# 输出结果
output/

# 日志文件（本地生成，按天轮转）
logs/

# 本地生成的数据文件（经验库/规则/参考数据，机器相关）
data/experience/
data/reference/
data/quota_rules/

# 定额Excel源文件（体积大，各机器不同，不纳入git）
data/quota_data/

# 文档导出（CHM解包等大量生成文件）
docs/chm_dump/

# 调试输出 / 临时文件
tests/debug_sheets.txt
_tmp_*.log
tmp_*.txt

# IDE配置
.vscode/
.idea/

# 系统文件
.DS_Store
Thumbs.db
nul
None/

# Claude Code 本地配置
.claude/

# local temp work dirs
.pytest-tmp/
jarvis-correct-*/
*.tar
*.lpk
tmp/
test_artifacts/
raw_files/
pytest_tmp_knowledge_commit/
.pytest_tmp_knowledge_commit/
.pytest_tmp_knowledge_promotion/
.tmp_selected_province.txt
.tmp_selected_aux_provinces.txt
data/*.db-wal
data/*.db-shm
tmp_match_pipeline_tail_backup.py.txt
replace_picker.py

# 备份和下载（非项目文件）
backups/
downloads/

# OSS下载工具（含硬编码凭证，不上传）
tools/download_oss_remaining.py

# OSS下载的样本数据（体积大）
data/oss_samples/
data/experience.db

# 采集/拉取历史（体积大，本地生成）
tools/.collect_history.json
tools/.pull_history.json

# Benchmark运行结果快照（本地生成，不该commit）
tests/benchmark_papers/_latest_result.json

# 向量模型目录（12GB，不上传）
models/

# PDF信息价数据（1.1GB，本地导入）
data/pdf_info_price/

# 训练数据和临时模型（大文件）
data/qwen3_training_triplets_v3.jsonl
data/qwen3_training_triplets_v4.jsonl
data/qwen3_exp_triplets_v4.jsonl
data/bill_library_all.json
data/bill_library.db
data/ltr_training_data.csv
data/ltr_model_v4_new.txt

# 自动生成的同义词 / 备份文件
data/auto_synonyms_*.json
data/*.bak
data/*.bak.*
*.bak.*

# 造价PDF源文件（体积大）
data/*.pdf

# Trae IDE / agent-browser 本地配置
.trae/
.agents/
skills-lock.json

# 根目录空数据库
experience.db
